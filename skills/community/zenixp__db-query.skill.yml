name: db-query
title: MYSQL QUERY
version: 1.0.0
description: Query project databases with automatic SSH tunnel management. Use when you need to execute SQL queries against
  configured databases, especially those accessible only via SSH tunnels. Automatically manages SSH connection lifecycle (establishes
  tunnel before query, closes after). Supports multiple dat
metadata:
  author: zenixp
  category: Development & Coding
  tags:
  - development-coding
  - query
  source: clawhub-community
  clawhub_slug: db-query
  clawhub_path: zenixp/db-query
  user_invocable: true
  created_at: '2026-02-16'
  updated_at: '2026-02-16'
dependencies:
  tools: []
implementation:
  type: markdown_prompt
  language: markdown
  content: '# MYSQL QUERY

    # Source: ClawHub community skill by zenixp

    # Install: clawhub install db-query

    # Path: zenixp/db-query


    ## Description

    Query project databases with automatic SSH tunnel management. Use when you need to execute SQL queries against configured
    databases, especially those accessible only via SSH tunnels. Automatically manages SSH connection lifecycle (establishes
    tunnel before query, closes after). Supports multiple dat


    ## Files

    - SKILL.md

    - _meta.json

    - scripts/INSTALL.md

    - scripts/config.example.json

    - scripts/db_query.py


    ## Installation

    ```bash

    clawhub install db-query

    ```'
examples:
- description: Install MYSQL QUERY from ClawHub
  usage: clawhub install db-query
schema_version: '1.0'
